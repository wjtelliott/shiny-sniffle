// Routes index is the same as the list index, with new logic, edits the order of the list.
import React from "react";
function CreateRoute() {
  return <p>Create new route here...</p>;
}

export default CreateRoute;
